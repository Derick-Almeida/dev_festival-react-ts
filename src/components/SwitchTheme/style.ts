import { styled } from "styled-components";

export const button = styled.button`
  border: none;
  border-radius: 50%;
  background: none;

  color: inherit;
  font-size: 1.5rem;

  display: flex;
  justify-content: center;
  align-items: center;
`;
